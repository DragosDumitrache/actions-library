name: 'dangerjs'
runs:
  using: composite
  steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - uses: borales/actions-yarn@v2.3.0
      with:
        cmd: install

    - name: DangerJS
      run: |
        yarn danger ci
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
